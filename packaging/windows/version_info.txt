# Windows EXE version resource for PyInstaller (--version-file format).
#
# Kept in sync manually with app/version.py (app.version.__version__) - there are
# two independent places on purpose: this file must stay in PyInstaller's
# VSVersionInfo Python-literal syntax, which cannot import the application package
# during the build. When bumping app/version.py, update filevers/prodvers and the
# FileVersion/ProductVersion strings below to match.
#
# No CompanyName entry: no real publisher/company value exists anywhere in this
# project, and none is invented here (see docs/DECISIONS.md).
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(0, 7, 0, 0),
    prodvers=(0, 7, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          u'040904B0',
          [
            StringStruct(u'FileDescription', u'Endpoint Toolbox'),
            StringStruct(u'FileVersion', u'0.7.0.0'),
            StringStruct(u'InternalName', u'EndpointToolbox'),
            StringStruct(u'OriginalFilename', u'EndpointToolbox.exe'),
            StringStruct(u'ProductName', u'Endpoint Toolbox'),
            StringStruct(u'ProductVersion', u'0.7.0.0'),
          ],
        )
      ]
    ),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])]),
  ],
)
